package dev.slne.surf.clan.core.common.utils

object PermissionRegistry {
    private const val PREFIX = "surf.clan"
    private const val COMMAND_PREFIX = "$PREFIX.command"

    val CLAN_COMMAND = "$COMMAND_PREFIX.clan"
}
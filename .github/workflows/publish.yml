name: Publish to Maven & Create GitHub Release


on:
  push:
    branches:
      - version/**
  workflow_dispatch:

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@master
    with:
      modules: "surf-clan-paper-*-all.jar;surf-clan-velocity-*-all.jar;surf-clan-placeholder-expansion-*-all.jar;surf-clan-microservice-*-all.jar"
    secrets: inherit